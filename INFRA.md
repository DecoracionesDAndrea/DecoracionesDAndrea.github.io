decoraciones-dandrea/
├── public/              # Archivos estáticos (favicon, robots.txt)
│   └── images/          # Todas tus imágenes aquí (LFC.png, etc.)
├── src/
│   ├── assets/          # Estilos globales y fuentes
│   ├── components/      # Componentes reutilizables (Navbar, Footer, Card)
│   ├── data/            # Aquí mueves tu 'titulos.json' como un archivo JS/JSON
│   ├── hooks/           # Lógica lógica compartida (ej. el control del Dark Mode)
│   ├── pages/           # Vistas completas (Home.jsx, About.jsx, Events.jsx)
│   ├── App.jsx          # Enrutador principal
│   └── main.jsx         # Punto de entrada
├── index.html           # Plantilla base (limpia, sin scripts pesados)
└── vite.config.js       # Configuración para el despliegue en GitHub Pages